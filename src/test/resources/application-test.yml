spring:
  redis:
    port: 16379
    host: localhost

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test
    username: sa
    password:

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: test20230522@gmail.com
    password: test1234
    properties:
      mail:
        smtp:
          starttls:
            enable: true
          auth: true
